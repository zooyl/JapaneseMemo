# Database
# https://docs.djangoproject.com/en/2.1/ref/settings/#databases


# change database name, user and password for your database, then change file name to local_settings.py

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': 'database name',
        'USER': 'username',
        'PASSWORD': 'username password',
        'HOST': 'localhost'
    }
}

# Secret key generated by generator

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'tsydsp79$3nblzyx3mw($#jxjit88(i%kn0r1c81j2wv5^re4j'


# For password reset using e-mail service, change data below
# I recommend setting up two-factor authentication on google.
# Go to your google account settings, security tab.
# Set up 2-Step Verification for your e-mail, and now you should have "App passwords" as a new element.
# Create new "app passwords", and use it as password down below.

email = "your_mail@service.com"
email_pass = "email_password"
